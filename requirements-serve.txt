# Serving + RAG dependencies (kept separate from the research requirements.txt).
# The research environment is unchanged; install this file for the moderation service.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
chromadb>=0.5
# Embeddings. Pulls torch; on a CPU-only box install the CPU wheel first if you
# want a smaller image (see docker/Dockerfile). If this package (or its model
# cache) is unavailable at runtime, the service falls back to a deterministic
# hashed bag-of-words embedder so it still runs offline (see rag/embeddings.py).
sentence-transformers>=3.0
